# ========================================
# GranjaTech - Environment Variables
# ========================================
# Copie este arquivo e renomeie para .env
# Ajuste os valores conforme necessário

# ========================================
# PostgreSQL Database
# ========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=GranjaTechDb

# ========================================
# Backend (.NET) - Connection String
# ========================================
# Formato: Host=<host>;Port=<port>;Database=<db>;Username=<user>;Password=<pass>
ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=GranjaTechDb;Username=postgres;Password=your_secure_password_here

# ========================================
# JWT Configuration
# ========================================
# IMPORTANTE: Gere uma chave forte e única (mínimo 32 caracteres)
# PowerShell: [System.Guid]::NewGuid().ToString() + [System.Guid]::NewGuid().ToString()
Jwt__Key=your-super-secret-jwt-key-minimum-32-characters-change-this
Jwt__Issuer=GranjaTechAPI
Jwt__Audience=GranjaTechApp

# ========================================
# CORS - Allowed Origins
# ========================================
# Separe múltiplas origens com ponto e vírgula
AllowedOrigins=http://localhost:3000;http://localhost:80;http://frontend:80

# ========================================
# Swagger Configuration
# ========================================
Swagger__Enabled=true

# ========================================
# Frontend (React)
# ========================================
REACT_APP_API_URL=http://localhost:5099/api

# ========================================
# pgAdmin (Apenas para Desenvolvimento)
# ========================================
PGADMIN_DEFAULT_EMAIL=admin@granjatech.com
PGADMIN_DEFAULT_PASSWORD=admin123

# ========================================
# Docker Configuration
# ========================================
COMPOSE_PROJECT_NAME=granjatech
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# ========================================
# NOTAS IMPORTANTES:
# ========================================
# 1. Nunca commite o arquivo .env real (com senhas reais) no Git
# 2. Use senhas fortes em produção
# 3. O .env deve estar no .gitignore
# 4. Para produção, use secrets management (Azure Key Vault, AWS Secrets Manager, etc.)
# 5. Gere uma nova chave JWT única antes de usar